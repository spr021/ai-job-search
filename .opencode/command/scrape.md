---
description: Find new job postings matching your profile via the installed portal-search CLIs, deduplicated across runs.
---

# /scrape - Find New Job Postings

Run a job search now: follow `.opencode/skills/job-scraper/SKILL.md` exactly, including its `search-queries.md` and the portal-search CLIs under `.agents/skills/`.

Input for this run (used wherever the workflow refers to `$ARGUMENTS`; may be empty): $ARGUMENTS
